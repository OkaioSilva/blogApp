<div class="card">
    <div class="card-body">
    <h4>Categorias: </h4>
        {{#each categorias}}
            {{!-- <a href="/categorias/{{slug}}" class="btn btn-dark"><h5 class="mt-2">Nome: {{nome}}</h5></a> --}}
            <a href="/categorias/{{slug}}"><h5 class="mt-2">Nome: {{nome}}</h5></a>
        {{else}}
        {{/each}}
    </div>
</div>